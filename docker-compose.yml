version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/media:/app/media
      - ./backend/temp:/app/temp

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend